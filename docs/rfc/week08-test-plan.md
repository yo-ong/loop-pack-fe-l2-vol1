# RFC: Week 08 — 테스트 설계: 무엇을 어느 층에서 지킬 것인가

브랜치 `feat/week-08` · 상태: 2단계 구현·3단계 실험 완료 · 작성 2026-08-21

**이 문서의 구성** — §1~§5는 1단계 설계(테스트 코드보다 먼저 커밋됨). 부록 A(3단계 실험 기록)와 부록 B(구현 중 설계 이탈 기록)는 구현·검증 후 추가된 실측 기록이다.

커버리지 숫자가 아니라 **지키기로 한 동작**을 고정한다. 검증 항목 15개를 단위·통합·E2E에 배치하고 근거를 남긴다. 이 문서는 테스트 코드보다 먼저 커밋한다(과제 규칙). 2단계(구현)와 3단계(자가 검증)는 §5의 계획을 그대로 따른다.

**빨리 훑을 때** — 배치표 §2.3, 애매했던 판단 2개 §2.4, 목록 밖 취사선택 §2.5, 공개 동작의 정의 §4, 2·3단계 계획 §5.

---

## 1. R — Requirements

**지킬 것** — 과제 제공 14개 + 직접 고른 순수 로직 1건(§2.2 — `buildTitle`·`buildDescription`). 15개 전부 이번 주에 구현한다.

**출발점(0단계, 커밋 완료)** — 이 설계는 아래 인프라 위에 선다. 여기서 정한 경계가 §2.3 배치의 전제다.

| 인프라          | 내용                                                                                                                    |
| --------------- | ----------------------------------------------------------------------------------------------------------------------- |
| vitest projects | `node`(기존 5개 + 새 단위) / `dom`(jsdom) 분리. DOM 테스트는 `*.dom.test.tsx` 파일 컨벤션                               |
| MSW             | `setup.dom.ts`에서 `onUnhandledRequest: "error"` — 그물 밖 요청은 조용히 나가지 못한다. 기본 핸들러는 성공 경로만       |
| render 헬퍼     | `renderWithProviders` — 테스트마다 새 `QueryClient({ retry: false })` + `NuqsTestingAdapter(searchParams, onUrlUpdate)` |
| 격리            | `afterEach`: `server.resetHandlers()` · `cleanup()` · cart/wishlist 스토어 리셋                                         |
| E2E             | Playwright chromium, `webServer: pnpm start`(production build 위). `pnpm check`의 마지막 단계                           |

**제약** — 기존 node 테스트 5개 불변 / 단위는 DOM 없이 통과해야 함 / 통합의 네트워크는 MSW로만 가로챔(HTTP 클라이언트 바꿔치기 금지) / E2E는 조건 기반 대기만 / 항목마다 정상 + 경계 케이스.

**하지 않을 것** — 커버리지 목표, 스냅샷 검증, 테스트를 위한 기능 변경. 특히 새로고침 시 찜·담기 초기화는 persist 미사용의 의도된 결과이므로 "지키기로 한 스펙"으로 고정한다(§2.3 15번 경계).

## 2. A — Architecture: 배치와 근거

### 2.1 배치 기준

검증하려는 **사실이 어느 층에 사는가**로 정한다.

1. 사실이 **함수의 입출력**이면 단위. 렌더가 필요했다면 배치가 틀린 것이다.
2. 사실이 **여러 층의 협업**(useQuery 상태 머신 ↔ 컴포넌트 분기 ↔ URL 어댑터 ↔ 스토어 구독)이어야만 성립하면 통합. jsdom + MSW로 재현 가능한 범위까지가 통합의 영토다.
3. 사실이 **브라우저 소유**(히스토리 스택, 진짜 reload, 서버 렌더 → hydration 전 경로)면 E2E. production build + mock 500ms 고정 지연 위라 회당 비용이 가장 크다 — 여기서만 지킬 수 있는 것만 올린다.

같은 항목이 두 층에 걸치면 **아래층으로 최대한 내리고, 위층에는 아래층이 못 보는 사실만 남긴다**. 11·12(통합)와 13·14·15(E2E)의 분담이 이 규칙의 적용례다(§2.4).

### 2.2 순수 로직 선택 — `buildTitle` · `buildDescription`

후보 3개를 두 질문으로 비교했다 — "**15개 그물에 이미 걸리는가**"(회귀가 다른 항목의 빨간불로 관찰될 수 있는가), "**내가 쓴 분기가 얼마나 있는가**"(테스트가 지키는 게 내 로직인가 라이브러리인가).

| 후보                              | 그물 밖인가                                                                                                                                                                                                            | 분기 밀도                                                                                                    | 결정     |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | -------- |
| ⓐ `parseAsPositiveInteger`        | 아니오 — 11번(재진입)이 파싱 결과를 select 값·목록으로 단언하고, 13·14번도 서버 렌더의 `loadProductSearchParams`를 지나며 URL·화면 단언에 걸린다                                                                       | `"0"`·`"-3"`·`"abc"`→null, `"1.5"`→1(parseInt) 등 풍부                                                       | 기각     |
| ⓑ `serializeProductsUrl`          | 예 — canonical 직렬화는 metadata 전용 경로                                                                                                                                                                             | 낮음 — nuqs `createSerializer` 설정 한 줄이라 지키는 건 대부분 라이브러리 동작                               | 기각     |
| ⓒ `buildTitle`·`buildDescription` | **예 — 어떤 테스트도 결과를 단언하지 못한다.** E2E가 production 서버에서 `generateMetadata`를 실행하긴 하지만 title·description 단언이 없고, `page.tsx`의 try/catch가 예외까지 삼켜 **크래시조차 빨간불이 될 수 없다** | trim, `page >= 2` 접미사, 카테고리 id→이름 해석과 fallback, 정렬 라벨 fallback, `totalCount === 0` 문구 분기 | **채택** |

잘못돼도 화면 기능은 멀쩡하고 검색엔진·공유 카드에만 노출되는 로직이라 **사람이 가장 늦게 발견하는 종류의 회귀**다 — 테스트가 아니면 지킬 수 없는 스펙이라는 점이 채택의 핵심 근거다.

**추출 선행** — 두 함수는 현재 `src/app/products/page.tsx`의 모듈 프라이빗이다. `src/_pages/products/lib/metadata.ts`로 옮기고 슬라이스 index에 공개한 뒤 page가 import한다(app→`_pages`는 index로만 진입 — 6주차 딥 임포트 차단 규칙). app 레이어는 라우팅 전용이고 이 함수들은 `ProductSearchState`와 응답 계약을 아는 페이지 소유 로직이라, 테스트를 떠나서도 자리가 lib이 맞다. 동작 보존 이동이며 2단계 단위 테스트 커밋에 선행한다.

### 2.3 검증 방법 표

| #   | 검증 대상                                  | 방법론 | 그렇게 정한 이유                                                                                                                                                                                                                                                                                                                                                                     | 빨간불이 되면 알게 되는 것                                                                                                                     |
| --- | ------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 장바구니·위시리스트 개수 파생              | 단위   | `createSelectionStore`의 `toggle`은 DOM·네트워크 없는 순수 상태 전이. 팩토리를 테스트마다 새로 만들면 격리가 공짜고, "이전 `ids`와 참조가 다른 Set"이라는 구독 계약은 이 층에서만 단언할 수 있다(§2.4-②)                                                                                                                                                                             | 담기/해제의 개수 계산 규칙 자체가 틀렸다 — 헤더·버튼이 아니라 상태 전이 층의 문제                                                              |
| 2   | URL 조건 → query key                       | 단위   | `productListQueries.list()`는 동기 함수고 캐시 정체성은 queryKey 동등성이 전부다 — 반환값 비교로 끝난다. `q.trim()` 정규화가 이 파일의 존재 이유                                                                                                                                                                                                                                     | 같은 조건이 다른 캐시를 봐서 중복 요청·깜빡임이 생기거나, 다른 조건이 캐시를 공유해 엉뚱한 목록이 보인다                                       |
| 3   | `buildTitle`·`buildDescription` ★직접 선택 | 단위   | 검색 상태·응답 → 문자열의 순수 함수인데 15개의 어떤 테스트도 결과를 단언하지 못한다(§2.2). 분기: 공백 q trim, `page >= 2` 접미사, 카테고리 id→이름 해석 fallback, `totalCount === 0` 문구                                                                                                                                                                                            | 검색 결과·페이지별 SEO 제목/설명이 잘못 노출된다 — 화면은 멀쩡해서 사람이 가장 늦게 발견하는 회귀                                              |
| 4   | 목록 로딩 → 성공                           | 통합   | 스켈레톤→데이터 전환은 useQuery 상태 머신과 `isPending` 분기의 협업 — 어느 한 층만으로는 성립하지 않는다. 경계: 성공 후 조건 변경 재조회에서는 스켈레톤으로 회귀하지 않고 이전 목록이 유지된다(`keepPreviousData`)                                                                                                                                                                   | 사용자가 목록을 영영 못 본다(로딩에 갇히거나 성공 데이터가 안 그려진다) / 조건 바꿀 때마다 화면이 깜빡인다                                     |
| 5   | 목록 빈 결과                               | 통합   | `totalCount === 0`(빈 결과)과 `totalCount > 0 && products.length === 0`(범위 밖 페이지)은 **다른 분기·다른 UI**다(`product-list-results.tsx:20`·`:25`). 분기 자체는 props로도 재현되지만, 응답 → useQuery → 분기의 배관까지 `ProductListContent` 한 렌더에서 함께 고정하려고 MSW 오버라이드로 통합에 둔다                                                                            | 결과 없음 안내 대신 빈 화면·스켈레톤이 보이거나, 빈 결과와 범위 밖 페이지의 구분이 사라진다                                                    |
| 6   | 목록 에러                                  | 통합   | MSW 500 오버라이드가 가장 싼 재현. 서버 `message`가 `CommerceApiError`를 거쳐 `role="alert"`에 그대로 노출되는 경로까지 지킨다. 경계: message 없는 500 → 기본 문구 `요청을 처리하지 못했습니다.`                                                                                                                                                                                     | 오류 상황에서 안내 없이 빈 화면이 뜨거나, 서버가 준 원인 문구가 중간에 소실된다                                                                |
| 7   | 에러에서 재시도로 복구                     | 통합   | `{ once: true }` 핸들러로 "1회 실패 후 성공"을 만들면 에러 UI → 클릭 → refetch → 성공까지 시나리오가 완결된다(wrapper의 `retry: false`가 전제). 경계: 캐시가 있는 상태의 갱신 실패는 전체 에러 화면이 아니라 배너 + 기존 목록 유지(`isError && data === undefined` 분기)                                                                                                             | 다시 시도 버튼이 장식이 된다 / 갱신 실패가 멀쩡히 보이던 목록까지 지운다                                                                       |
| 8   | 카테고리 변경 → 목록 변경                  | 통합   | select 조작 → 요청 파라미터 → 목록 교체가 한 흐름이고, 픽스처 이름(`casual-1`)이 카테고리를 드러내 교체를 이름으로 단언할 수 있다. 경계: page 2에서 카테고리를 바꾸면 1페이지로 복귀(`product-filters.tsx`의 `page: 1` 패치)                                                                                                                                                         | 필터가 화면에만 있고 동작하지 않거나, 이전 페이지 번호가 새 필터에 끌려가 빈 페이지가 보인다                                                   |
| 9   | 정렬 변경 → 순서 변경                      | 통합   | 8과 동형이되 단언이 다르다 — heading 텍스트의 **순서 배열**을 비교한다. 포함 여부만 보면 정렬 검증이 아니다. ⚠ 검증 쌍은 **최신순 → 높은 가격순**으로 고정한다 — 픽스처상 최신순과 낮은 가격순은 **완전히 동일한 순서**라(§3) 이 쌍으로 짜면 정렬이 통째로 무시돼도 초록불이다. 경계: 정렬 변경 시 page 1 복귀(`product-filters.tsx`의 정렬 onChange에도 별도 `page: 1` 패치가 있다) | 정렬을 바꿔도 순서가 그대로다 / 이전 페이지 번호가 새 정렬에 끌려간다                                                                          |
| 10  | 페이지 이동 → 목록 변경                    | 통합   | 다음/이전 클릭 → 목록 교체와 경계(1페이지에서 `이전` disabled, 마지막에서 `다음` disabled)를 한 파일에서 지킨다                                                                                                                                                                                                                                                                      | 페이지네이션이 이동하지 않거나 경계 밖으로 나간다                                                                                              |
| 11  | 조작이 URL에 반영 · URL로 재진입           | 통합   | 이 항목의 사실은 "조작 ↔ URL 문자열"의 계약이다. 반영은 `onUrlUpdate`로, 재진입은 `searchParams` 주입으로 — 양방향 모두 `NuqsTestingAdapter`가 재현한다(§2.4-①). 경계: 기본값 조건은 URL에서 생략                                                                                                                                                                                    | 공유한 URL이 다른 화면을 보여준다 — "URL이 검색 조건의 SoT"라는 전제의 붕괴                                                                    |
| 12  | 담기 → 헤더 개수 · 다시 누르면 빠짐        | 통합   | 조작은 **찜(위시리스트) 버튼** 기준이다 — 항목명의 "담기"는 카드 토글 행위 일반을 가리키고, 코드에서 담기/담김은 장바구니 버튼의 텍스트다(§4 표). 버튼과 헤더는 스토어 구독으로만 연결된다 — 브라우저 불필요. 한 wrapper에 `HeaderActions` + `ProductListContent`를 함께 렌더(§2.4-②). 경계: 서로 다른 상품 2개 찜 → `위시리스트 2`, `장바구니 0` 유지(두 스토어 분리)               | 찜이 헤더 개수에 반영되지 않거나 위시리스트·장바구니 두 스토어가 섞인다                                                                        |
| 13  | 뒤로·앞으로 가기로 필터 복원               | E2E    | 진짜 히스토리 스택은 브라우저에만 있다. `history: "push"`의 실효성은 `goBack`/`goForward`로만 증명된다. 경계: 카테고리→정렬 두 번 조작 후 뒤로 2회에 최초 진입 URL(쿼리 없음)까지 복원되고, 앞으로 가기로 각 단계가 재적용된다                                                                                                                                                       | 뒤로가기가 필터 변경을 건너뛰거나 잃는다 — 히스토리에 쌓이지 않고 교체되고 있다                                                                |
| 14  | 새로고침해도 필터 상태 유지                | E2E    | 진짜 reload와 서버 렌더 경로(`loadProductSearchParams`)가 필요하다. 스토어에 persist가 없으니 URL 말고는 살아남을 곳이 없다는 것의 증명. 경계: 비기본값 2개를 설정한 상태로 reload — select 값·목록이 조건과 일치하고, 기본값 조건은 reload 후에도 URL에 생기지 않는다                                                                                                               | 새로고침하면 조건이 초기화된다 — URL 밖 어딘가에 상태가 새고 있다                                                                              |
| 15  | 목록 진입 → 담기 → 헤더 확인               | E2E    | 핵심 사용자 여정의 스모크(조작은 12번과 같이 찜 버튼 기준). production build 위에서 서버 렌더 → hydration → 스토어 → 헤더의 전 스택을 관통한다. 경계: 새로고침하면 0으로 복귀 — persist 미사용도 지키기로 한 스펙(§1)                                                                                                                                                                | 12번(jsdom)이 초록인데 여기만 빨간불이면 jsdom이 못 보는 구간 — production 번들·서버 렌더→hydration 전환 — 이 깨진 것. 배포를 막아야 하는 신호 |

### 2.4 애매했던 판단 2개

**① 11번 — 통합인가 E2E인가.** "URL"이라는 단어가 주소창을 연상시켜 13·14와 같은 E2E 묶음으로 보내고 싶어지는 항목이다. 그러나 이 항목이 고정하려는 사실은 주소창이 아니라 **컴포넌트 조작과 URL 문자열 사이의 계약**이다 — 반영 방향(카테고리 변경 → `category=casual` + `page` 리셋 직렬화, 기본값 생략)과 재진입 방향(`?category=casual&sort=price-desc` → 파싱 → select 값·목록 초기 상태. ⚠ `casual&page=2` 같은 조합은 금물 — casual은 5개뿐이라 어떤 pageSize에서도 2페이지가 없어 "존재하지 않는 페이지" placeholder가 뜬다). 이 양방향을 `NuqsTestingAdapter`가 `onUrlUpdate`와 `searchParams`로 그대로 재현하므로 통합으로 내렸다. 브라우저만 소유한 사실 — 히스토리 스택(뒤로·앞으로), reload + 서버 렌더 — 은 이 계약 밖이고, 정확히 13·14번의 자리다. 반대로 11번까지 전부 E2E로 몰았다면: production build + 500ms 고정 지연 위에서 경계 케이스(기본값 생략, 재진입 시 select 값·목록 일치)를 늘릴 때마다 회당 비용이 쌓이고 13·14와 시나리오가 중복된다. 통합 선택으로 잃는 것도 있다 — 테스트 어댑터와 진짜 주소창의 간극(nuqs 자체 결함)은 통합이 못 잡는다. 그 리스크는 13·14가 실제 주소창을 한 번씩 지나가며 상쇄한다.

**② 1번 단위와 12번 통합 — 같은 "개수"를 두 번 지키는 게 아닌가.** 겹쳐 보이지만 두 테스트가 고정하는 사실이 다르다. 단위(1번)는 **전이 규칙**(같은 id 두 번 → 제거, 다른 id 두 개 → 2)과 **참조 교체 계약**(toggle 후 `ids`는 이전과 다른 Set)을 지킨다. 특히 참조 계약은 현재 소비처가 전부 원시값 셀렉터(`ids.size`, `ids.has(id)`)라서 **직접 변경으로 깨뜨려도 화면에는 증상이 없다** — Set 자체를 구독하는 미래 소비처가 생기는 순간 터지는 시한폭탄이고, 이걸 단언할 수 있는 층은 단위뿐이다. 통합(12번)은 **구독·표시 경로**를 지킨다 — 스토어가 완벽해도 헤더 셀렉터가 `ids.size`를 읽지 않으면(고정값 렌더, cart↔wishlist 스왑) 단위는 전부 통과한다. 단위를 빼면 참조 계약이 무방비가 되고 실패 시 원인 폭(스토어인가 구독인가 렌더인가)이 넓어지며, 통합을 빼면 "스토어는 맞는데 화면이 틀린" 회귀에 무방비가 된다. 둘은 중복이 아니라 원인 축의 분담이다.

### 2.5 목록 밖 취사선택

**다음에는 할 것 — `ProductSearchInput`의 디바운스·flush·IME 처리** (`src/_pages/products/ui/product-search-input.tsx`). commit·cancel 레퍼런스 관리, Enter의 `flush()`, `isComposing` 가드, 외부 value 동기화(`lastCommittedRef`)가 얽힌 실제로 복잡한 로직인데 이번 15개는 이 경로를 지나지 않는다(8·9·11 모두 select 조작으로 설계했고, E2E도 검색어 타이핑이 없다). 변경 빈도 — 검색 UX는 요구가 자주 바뀌고 타이머·ref가 얽혀 있어 회귀가 잦은 영역. 실패 비용 — 검색이 아예 안 되거나 한글 조합 중 커밋되면 IME 사용자 전원이 겪는다. fake timer가 필요해 대기 규칙(§5.2)이 다른 별도 파일이 되므로 이번 범위에서 제외하고 다음 순번 1위로 둔다.

**앞으로도 안 할 것 — `Placeholder`·`ToggleButton` 단독 렌더 테스트** (`src/shared/ui/`). props를 그대로 그리는 표시 전용 공통 컴포넌트다. 변경 빈도 — 6주차 이후 무변경. 실패 비용 — 틀리면 이들을 실사용 맥락에서 지나는 통합 5·6·12번이 즉시 빨간불이 된다. 단독으로 테스트하면 "렌더된다" 수준의 보장 없는 단언이 되기 쉽고, 프로젝트 규칙상 공통 컴포넌트는 비즈니스 로직을 갖지 않으므로 지킬 분기 자체가 없다.

## 3. D — Data: 모킹 경계와 픽스처

**경계 원칙** — 모킹은 HTTP 한 곳에서만 한다. 앱 코드(`fetchCommerceApi`)는 무수정으로 실제 fetch를 발사하고 MSW가 가로챈다. 스토어·컴포넌트·URL 어댑터는 실물을 쓴다.

| 실물                                  | 테스트에서의 대체물                                                                              | 근거                                                                                                            |
| ------------------------------------- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- |
| 네트워크(`/api/products`·`/api/home`) | 통합: MSW. 기본 핸들러는 성공만, 빈·에러·1회 실패는 **그 테스트 안에서** `server.use` 오버라이드 | 예외 상태가 파일 기본값이 되면 다른 테스트가 오염된다. `onUnhandledRequest: "error"`로 그물 밖 요청은 즉시 실패 |
| URL                                   | 통합: `NuqsTestingAdapter`(`searchParams` 주입 · `onUrlUpdate` 관찰) / E2E: 진짜 주소창          | 어댑터가 못 가진 것(히스토리·reload)은 13·14가 담당(§2.4-①)                                                     |
| 전역 스토어(zustand)                  | 모킹 없음 — 단위는 `createSelectionStore()` 새 인스턴스, 통합은 실물 + `afterEach` 리셋          | 스토어 자체가 검증 대상이다. 전역 스토어를 단위 테스트에서 import하면 격리가 깨진다                             |
| Query 캐시                            | 테스트마다 새 `QueryClient({ retry: false })`                                                    | 캐시 공유는 테스트 간 결합을 만들고, 기본 재시도(3회 백오프)는 에러 테스트를 타임아웃시킨다                     |
| E2E 전 구간                           | 모킹 0 — production build + 실제 mock 라우트(500ms 고정 지연)                                    | Playwright의 web-first assertion(자동 재시도)이 지연을 흡수한다. `waitForTimeout` 금지                          |

**픽스처 설계와 함정 실측** — 결정적 카탈로그 25개(5카테고리 × 5). 이름 `casual-1`이 카테고리·순번을 드러내 8번의 "목록 교체"를 이름으로 단언할 수 있다. 정렬 축을 실측하면: `price = 1000 × serial`(순번 비례), `createdAt = 30 − serial`(순번 반비례)로 두 축이 **정확히 역상관**이다. 그 결과 **최신순과 낮은 가격순은 전 구간에서 동일한 순서**를 만들고(카테고리를 걸어도 동일), 단일 카테고리 안에서는 인기순(`reviewCount = (5 − i) × 10 + 카테고리 오프셋`)까지 같은 순서다. 9번의 검증 쌍을 최신순 → **높은 가격순**(전체 역순)으로 고정한 근거이며, 동순서 쌍(최신순↔낮은 가격순)을 골랐다면 정렬 파라미터가 통째로 무시돼도 통과하는 공허한 테스트가 됐을 것이다. 카테고리 5개짜리 목록에는 2페이지가 없다는 함정(§2.4-①)도 여기서 나온다. `buildProduct(overrides)` 패턴이라 개별 테스트가 필요한 필드만 고칠 수 있다.

## 4. I — Interface: 공개 동작의 정의

무엇이 공개 동작이고 무엇이 구현 세부인지 — 층마다 계약이 다르다.

**단위의 공개 계약** — export된 함수의 입출력. queryKey는 내부 배열 모양이 아니라 **동등성**만 계약이다(같은 조건 → deep equal, 다른 조건 → 다른 키). 예외 하나: `ids`의 참조 교체는 내부 구현처럼 보이지만 zustand 구독 알림의 전제 계약이므로 단언한다 — 근거는 §2.4-②, 검증은 §5.3 실험 2.

**통합의 공개 계약** — 접근성 트리(role·label·텍스트)와 URL 문자열. 아래 실측 표의 속성만으로 15개가 커버되므로 `getByTestId`는 필요 없다고 예측한다 — 쓰게 되면 왜 다른 방법이 안 됐는지 기록한다(과제 규칙).

**구현 세부(단언 금지)** — 컴포넌트 내부 state·훅 호출 여부, queryFn 호출 횟수, 스토어 셀렉터 함수의 모양, CSS 클래스(`week05-*`), 스켈레톤 내부 구조.

**셀렉터 실측표** (코드가 이미 제공하는 접근성 속성)

| 화면 상태      | 셀렉터                                                                                  | 출처                                                               |
| -------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 로딩           | `aria-label="상품 목록 불러오는 중"` (`aria-busy` section)                              | `product-list-content.tsx:31`                                      |
| 성공           | `총 {n}개` 텍스트 · 상품명 `role="heading", level: 2`                                   | `product-list-results.tsx:19` · `product-card.tsx`(`titleAs="h2"`) |
| 빈 결과        | `검색 결과가 없어요` (`role="status"`)                                                  | `product-list-results.tsx:22`                                      |
| 범위 밖 페이지 | `존재하지 않는 페이지예요` + `마지막 페이지로 이동` 버튼                                | `product-list-results.tsx:27`                                      |
| 조회 실패      | `role="alert"` + `상품을 불러오지 못했어요` + `다시 시도` 버튼                          | `product-list-content.tsx:35`                                      |
| 갱신 실패 배너 | `role="alert"` + `목록을 갱신하지 못했어요`                                             | `product-list-content.tsx:47`                                      |
| 필터           | label `검색`·`카테고리`·`정렬`·`페이지 크기` → `getByLabelText`                         | `product-filters.tsx` · `product-search-input.tsx`                 |
| 페이지네이션   | `nav aria-label="페이지 이동"` · `이전`/`다음` 버튼 · `{page} / {totalPages}`           | `product-list-results.tsx:47`                                      |
| 찜/담기 버튼   | `aria-label="{상품명} 위시리스트/장바구니"` · `aria-pressed` · 텍스트 찜/찜됨·담기/담김 | `toggle-button.tsx:19` · `wishlist-button.tsx:17`                  |
| 헤더           | `위시리스트 {n}` / `장바구니 {n}` 텍스트                                                | `header-actions.tsx:12`                                            |

**테스트 이름** — "조건 → 결과"가 드러나는 한국어 문장. `카테고리를 바꾸면 페이지가 1로 돌아간다`(O), `filters work`(X). 이름만 읽고 무엇이 깨졌는지 알 수 있어야 3단계 실험 기록(잡힌 테스트 이름)이 의미를 가진다.

## 5. O — Optimization: 구현 순서와 자가 검증 계획

### 5.1 파일 계획 (2단계)

피드백이 빠른 것부터 — 단위 → 통합 → E2E 순서로 구현하고 방법론 단위로 커밋한다.

| 방법론 | 파일                                                                                                                                                                                                                                                                                                  | 담당 항목 |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 단위   | `src/shared/lib/create-selection-store.test.ts`                                                                                                                                                                                                                                                       | 1         |
| 단위   | `src/_pages/products/api/queries.test.ts`                                                                                                                                                                                                                                                             | 2         |
| 단위   | `src/_pages/products/lib/metadata.test.ts` (※ `lib/metadata.ts` 추출 선행, §2.2)                                                                                                                                                                                                                      | 3         |
| 통합   | `src/_pages/products/ui/product-list-content.states.dom.test.tsx`                                                                                                                                                                                                                                     | 4·5·6·7   |
| 통합   | `src/_pages/products/ui/product-list-content.filters.dom.test.tsx`                                                                                                                                                                                                                                    | 8·9·10    |
| 통합   | `src/_pages/products/ui/product-list-content.url.dom.test.tsx`                                                                                                                                                                                                                                        | 11        |
| 통합   | `src/_pages/products/ui/wishlist-header-sync.dom.test.tsx` (※ 위치 정정 — 초판은 widgets/header의 스모크 파일 확장이었으나, widgets 파일은 상위 레이어 `@/_pages`를 import할 수 없다는 FSD lint 하네스가 테스트 파일에도 적용된다. 두 슬라이스를 함께 렌더하는 조합 지점은 상위인 `_pages` 쪽이 맞다) | 12        |
| E2E    | `e2e/filter-history.spec.ts`                                                                                                                                                                                                                                                                          | 13        |
| E2E    | `e2e/filter-reload.spec.ts`                                                                                                                                                                                                                                                                           | 14        |
| E2E    | `e2e/wishlist-journey.spec.ts`                                                                                                                                                                                                                                                                        | 15        |

### 5.2 대기·격리 규칙

- **통합 파일의 비동기 대기는 첫 전환 1회가 원칙** — `await screen.findByText(/총 \d+개/)` 이후는 동기 `getBy*`로 단언한다. 모든 검증을 `waitFor`로 감싸지 않는다(과제 규칙). 상호작용 후 목록 교체처럼 새 요청이 발생하는 지점만 `findBy*`를 추가한다.
- **격리** — 단위는 팩토리로 새 인스턴스를 만들고 전역 스토어를 import하지 않는다. 통합은 0단계 `afterEach`(핸들러·DOM·스토어 리셋)와 테스트별 새 QueryClient가 담당한다. 파일 단독 실행과 순서 변경에 결과가 무관해야 한다.
- **E2E** — `sleep`/`waitForTimeout` 금지. `expect(page).toHaveURL/toHaveText`의 자동 재시도가 mock 500ms 지연을 흡수한다.

### 5.3 뮤테이션 실험 계획 (3단계)

방법론마다 1개 이상, 한 번에 한 곳만 바꾸고 테스트 코드는 불변, 기록 후 `git checkout -- <file>`로 원복한다. E2E 실험은 build 재사용을 위해 대상을 정해 몰아서 돌린다.

| 방법론 | 대상                        | 변형                                        | 잡아야 하는 테스트(예상)                                                                                                                                  |
| ------ | --------------------------- | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 단위   | `queries.ts`                | `q: params.q.trim()` → `q: params.q`        | 2번의 trim 케이스                                                                                                                                         |
| 단위   | `create-selection-store.ts` | `new Set(state.ids)` 복사 제거, 직접 변경   | 1번의 참조 단언. **내용 검사만으로는 살아남고, 현재 소비처가 원시값 셀렉터라 12번 통합·E2E로도 못 잡는다**(§2.4-②) — 참조 단언이 유일한 그물임의 검증     |
| 단위   | `metadata.ts` (추출 후)     | `page >= 2` → `page > 2`                    | 3번의 2페이지 경계                                                                                                                                        |
| 통합   | `product-filters.tsx`       | 카테고리 onChange의 `page: 1` 삭제          | 8번의 경계 케이스만 잡을 수 있는 변형 — 눈에 안 띄는 곁다리의 전형                                                                                        |
| 통합   | `product-list-results.tsx`  | `page >= totalPages` → `>`                  | 10번의 disabled 경계                                                                                                                                      |
| 통합   | `product-list-content.tsx`  | `isError && data === undefined` → `isError` | 7번의 갱신 실패 배너 경계                                                                                                                                 |
| E2E    | `product-list-content.tsx`  | `history: "push"` → `"replace"`             | 13번만 잡을 수 있는 변형 — E2E 실험으로 제격                                                                                                              |
| E2E    | `header-actions.tsx`        | wishlistCount ↔ cartCount 스왑              | **12번(통합)이 먼저·싸게 잡고** 15번(E2E)도 잡는다 — 목적은 층별 그물 비교와 실패 메시지의 원인 추정력 기록. E2E만의 그물 증명은 위의 push→replace가 담당 |

실험마다 기록: 망가뜨린 곳 / 어떻게 / 잡힘·살아남음 / 실패한 테스트 이름 / 실패 메시지만 보고 원인을 알 수 있었나. 살아남은 변형은 테스트를 보강하고 이유를 남긴다.

사전 분석에서 걸러낸 변형 하나 — `product-list-results.tsx`의 `Math.max(1, …)` 제거는 **equivalent mutant**다: `Math.max`가 효력을 갖는 유일한 입력은 `totalCount === 0`인데 그 경우 빈 결과 분기가 먼저 렌더돼 `totalPages`가 화면 어디에도 쓰이지 않는다. 어떤 테스트도 못 잡는 게 정상이므로 실험 대상이 아니라 §5.4의 "죽일 수 없는 변형" 기록 예시로 둔다.

### 5.4 Advanced — Stryker (선택)

`@stryker-mutator/core` + `@stryker-mutator/vitest-runner`를 쓰되 **mutate 범위를 단위로 분류한 3파일로 한정**한다(`create-selection-store.ts`, `queries.ts`, `metadata.ts`). 통합이 걸리는 컴포넌트를 넣으면 변형 × DOM 렌더의 곱으로 실행이 끝나지 않는다 — 이것이 범위 한정의 근거다. 살아남은 변형 중 의미 있는 것 2개 이상 보강, 못 죽이는 변형(equivalent mutant)은 이유 기록(§5.3에서 사전 식별한 `Math.max` 사례가 첫 항목), 실행 시간과 상시 사용 여부 판단까지 남기면 완료.

---

# 부록 — 프로젝트 기록

## 부록 A. 3단계 실험 기록 (2026-08-21 실측)

프로토콜 — §5.3 계획 그대로 8개 전부 실행했다. 실험당 구현 한 곳만 변형, 테스트 코드 불변, 해당 방법론의 테스트 실행, 기록 후 `git checkout -- <file>`로 원복. E2E 실험 2건(7·8)은 각각 production build를 다시 해 몰아서 돌렸다. 최종 트리에 변형 코드 없음(전 스위트 78개 + E2E 6개 초록 확인).

| #   | 방법론 | 망가뜨린 곳                 | 어떻게 바꿨나                               | 결과                                                      | 실패한 테스트                                                                                          | 실패 메시지만으로 원인 판독                                                                                                      |
| --- | ------ | --------------------------- | ------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 단위   | `queries.ts`                | `q: params.q.trim()` → `q: params.q`        | 잡힘                                                      | `검색어의 앞뒤 공백은 정규화되어 같은 queryKey를 만든다` 외 1                                          | **가능** — diff에 `q: ' 셔츠 '`가 그대로 보여 정규화 소실임이 즉시 드러남                                                        |
| 2   | 단위   | `create-selection-store.ts` | `new Set(state.ids)` 복사 제거, 직접 변경   | 잡힘 — **참조 단언 1개만**                                | `toggle은 이전 상태의 ids와 참조가 다른 새 Set을 만든다`                                               | **가능** — `not to be Set{…} // Object.is equality` + 테스트 이름                                                                |
| 3   | 단위   | `metadata.ts`               | `page >= 2` → `page > 2`                    | 잡힘                                                      | `2페이지부터는 제목 뒤에 페이지 번호가 붙는다`                                                         | **가능** — `'상품 목록'` vs `'상품 목록 2페이지'`                                                                                |
| 4   | 통합   | `product-filters.tsx`       | 카테고리 onChange의 `page: 1` 삭제          | 잡힘                                                      | 8번 경계 `2페이지에서 카테고리를 바꾸면 1페이지로 돌아간다` + 예상 밖 보너스로 11번 URL 테스트까지 2개 | **절반** — 8번의 "`1 / 1`을 못 찾음"과 11번의 `expected '2' to be null`은 메시지 단독으론 모호하고 테스트 이름과 결합해야 판독됨 |
| 5   | 통합   | `product-list-results.tsx`  | `page >= totalPages` → `>`                  | 잡힘                                                      | `1페이지에서는 이전이, 마지막 페이지에서는 다음이 비활성화된다`                                        | **가능** — toBeDisabled 실패 + 이름으로 경계 문제임이 드러남                                                                     |
| 6   | 통합   | `product-list-content.tsx`  | `isError && data === undefined` → `isError` | 잡힘                                                      | `캐시가 있는 상태에서 재조회가 실패하면 기존 목록을 유지한 채 갱신 실패 배너가 보인다`                 | **가능** — 배너 문구 대신 전체 에러 화면이 잡힘                                                                                  |
| 7   | E2E    | `product-list-content.tsx`  | `history: "push"` → `"replace"`             | 잡힘 — **13번 2개만** (reload·여정 4개는 통과)            | `필터를 바꾼 뒤 뒤로 가기를 하면…` 외 1                                                                | **가능** — `Received string: "about:blank"` = 히스토리에 단계가 안 쌓여 뒤로가기가 사이트 밖까지 나감                            |
| 8   | E2E    | `header-actions.tsx`        | wishlistCount ↔ cartCount 스왑              | 잡힘 — 12번 통합 2개 + 15번 E2E 2개 (필터 E2E 4개는 통과) | 통합 `상품을 찜하면 헤더 위시리스트가 1이…` 외 3                                                       | **층별 차이** — 통합은 "`위시리스트 1`을 못 찾음"으로 짐작 가능, E2E는 `element(s) not found`라 판독력이 가장 낮음               |

**관찰 1 — 살아남은 변형은 0이지만, "쉬운 곳만 골랐다"는 뜻이 아니다.** 실험 2에서 참조 단언 하나를 빼고 보면 **나머지 78개 중 77개(12번 통합 포함)가 전부 통과**했다. 현재 소비처가 전부 원시값 셀렉터(`ids.size`·`ids.has`)라 zustand가 병합 객체를 새로 만들어 항상 notify하고, 셀렉터 결과(원시값)가 달라져 리렌더까지 정상 동작하기 때문이다 — §2.4-②의 예측이 실측으로 증명됐다. 이 변형은 Set 자체를 구독하는 소비처가 생기는 순간 터지는 시한폭탄이고, 설계 단계에서 선제한 참조 단언이 유일한 그물이었다. "살아남을 변형"을 구현 전에 찾아 단언을 심어둔 사례로 기록한다.

**관찰 2 — equivalent mutant는 사전 분석으로 걸렀다.** `Math.max(1, …)` 제거(§5.3 하단)는 실험 전에 관찰 불가능함을 논증하고 제외했다. Advanced에서 Stryker가 같은 변형을 만들어 살아남는지로 이 판정을 교차 확인한다.

**관찰 3 — 실패 메시지의 판독력은 단위 > 통합 > E2E.** 같은 결함(실험 8)에 대해 통합은 어떤 텍스트가 틀렸는지를, E2E는 "요소 없음"만 말한다. 원인 판독이 안 되는 층일수록 테스트 이름이 조건→결과를 정확히 말해야 한다는 §4 규칙의 근거가 됐다. 실험 4의 11번 실패(`expected '2' to be null`)도 이름이 없었다면 판독 불가였다 — 이름 덕에 도달했으므로 단언은 손보지 않는다.

## 부록 B. 구현 중 설계 이탈·발견 기록

1. **NuqsTestingAdapter의 `hasMemory`** — 어댑터는 기본값(`hasMemory: false`)에서 `onUrlUpdate` 콜백만 호출하고 **렌더된 searchParams 상태를 갱신하지 않는다**. 상호작용(8~12번) 테스트 8개가 전부 타임아웃으로 전멸한 원인. 0단계 스모크 테스트는 fetch·URL이 없어 이 미비를 드러낼 수 없었다. `renderWithProviders`에 `hasMemory`를 켜서 해결 — "조작이 URL 상태에 기억된다"가 통합 테스트의 전제라 헬퍼 기본값으로 고정했다.
2. **12번 테스트 파일 위치 정정** — §5.1 초판의 widgets/header 배치는 FSD lint(상위 레이어 import 금지)가 테스트 파일에도 적용돼 불가능했다. 두 슬라이스를 함께 렌더하는 조합 지점은 상위 레이어 몫이므로 `_pages/products/ui/wishlist-header-sync.dom.test.tsx`로 옮겼다(테스트 코드 작성 전 docs 커밋으로 선반영).
3. **7번 경계(갱신 실패 배너)의 트리거** — 같은 queryKey의 배경 재조회 실패는 UI에 트리거가 없어(실사용에선 창 포커스 재조회) `renderWithProviders`가 반환하는 queryClient로 `invalidateQueries()`를 호출해 재현했다. 단언은 화면(배너 role="alert" + 기존 목록 유지)으로만 한다.
4. **E2E 단언은 픽스처 무지 원칙** — E2E는 MSW가 아닌 실제 mock API를 타므로 `casual-1` 같은 픽스처 이름 단언 금지. 개수 텍스트를 조작 전에 캡처해 reload 후와 비교하는 조건 기반 단언으로 대체했다. 모든 상호작용 전에 클라이언트 쿼리 결과(`총 N개`)를 기다려 하이드레이션 경합도 차단.

## 부록 C. 0단계 기록 보완 — 환경 분리 실측 (2026-08-21)

체크리스트의 "환경 셋업 시간 비교" 기록이 없어 같은 스위트(13파일 78테스트)로 실측했다. 비교 방법: 현행 설정 그대로 한 번, node 프로젝트의 environment만 jsdom으로 바꾼 임시 설정으로 한 번(임시 설정 파일은 측정 후 삭제).

| 구성                                 | environment 누적 시간 | 전체 Duration |
| ------------------------------------ | --------------------- | ------------- |
| 분리(현행: node 8파일 + jsdom 5파일) | **5.34s**             | 3.70s         |
| 전부 jsdom (13파일)                  | **15.43s** (약 2.9배) | 3.55s         |

지금 규모에서는 워커 병렬화가 환경 비용을 흡수해 벽시계 시간 차이가 없지만, environment 누적 시간은 3배다 — DOM이 필요 없는 파일 8개가 각각 jsdom 부팅 비용을 새로 내기 때문. 파일이 늘수록 워커 수를 넘어서는 순간부터 벽시계 시간으로 전가된다. 분리 방식(파일 컨벤션 `*.dom.test.tsx` + vitest projects)의 근거는 §1 표 참조.

## 부록 D. Advanced — Stryker 전수 뮤테이션 기록 (2026-08-21 실측)

**설정** — `@stryker-mutator/core`·`@stryker-mutator/vitest-runner` 10.0.0. `stryker.config.json`에 `plugins`를 명시해 pnpm의 플러그인 자동 탐색 실패를 선제 회피했고(과제 힌트의 `Cannot find TestRunner plugin "vitest"` 케이스), `coverageAnalysis: "perTest"`로 변형을 덮는 테스트만 실행되게 했다. mutate 범위는 §5.4 그대로 단위 3파일.

**범위 한정의 실측 근거** — 단위 3파일에서 변형 56개, 전체 실행 **23~25초**(변형당 평균 2.39개 테스트 실행). 통합이 걸리는 컴포넌트를 mutate에 넣으면 변형 수가 수백 개로 늘고 변형마다 jsdom 렌더 스위트가 도는 곱셈이라 이 시간이 성립하지 않는다.

**1차 실행** — 56개 중 53 사멸 / 3 생존 / 점수 94.64%.

| 생존 변형                                  | 위치             | 의미가 바뀌는가                                                                                                                                                                                                                                                                                      | 조치                                                                                                                                                                                                        |
| ------------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `staleTime: 60 * 1000` → `60 / 1000`       | `queries.ts:10`  | **예** — 캐시 신선도가 1분 → 0.06ms가 되면 조건 변경·재렌더마다 재요청이 나간다. 6주차 RFC §5.1에서 "유지"라고 선언한 캐시 정책인데 지키는 단언이 없었다                                                                                                                                             | 보강 — `목록 쿼리는 1분 staleTime 캐시 정책을 유지한다` 추가                                                                                                                                                |
| `category.id === search.category` → `true` | `metadata.ts:14` | **예** — "id가 일치하는 카테고리"가 "목록의 첫 번째 아무거나"가 된다. 살아남은 원인은 테스트 픽스처의 categories가 **1개뿐**이라 두 의미가 구분 불가능했던 것 — §3 픽스처 함정(동순서 정렬)과 같은 계열의 설계 결함                                                                                  | 보강 — 카테고리 2개 픽스처에서 두 번째 것을 찾는 `여러 카테고리 중 검색 조건과 id가 일치하는 것의 이름을 고른다` 추가                                                                                       |
| `…find(…)?.label` → `.label`               | `metadata.ts:17` | **아니오 — equivalent mutant.** `search.sort`는 `parseAsStringLiteral`이 유효값만 만들고 `sortFilterOptions`는 전 값을 포함한 정적 목록이라 find가 실패하는 입력이 존재하지 않는다. 타입이 이미 보장하는 방어 코드라 의미 불변. 죽이려면 타입 계약 밖 입력을 주입해야 하는데 그건 지킬 스펙이 아니다 | 기록만. 대조: category 쪽 같은 모양의 fallback은 죽었다 — `result.categories`는 **런타임 데이터**(서버 응답)라 빈 목록 테스트가 실제로 존재하기 때문. 방어 코드의 생사는 모양이 아니라 입력의 출처가 가른다 |

**2차 실행(보강 후)** — 56개 중 55 사멸 / 1 생존(위 equivalent) / 점수 **98.21%**, 25초.

**손 실험(부록 A) 대비 도구가 추가로 찾아준 것** — staleTime과 find 조건 2건 모두 손 실험 8개에 없던 발견이다. 공통점은 "실행은 되지만 아무도 단언하지 않는 사실"이라는 것 — 손 실험은 내가 의심한 곳만 확인하고, 전수 도구는 의심하지 않은 곳을 찾아준다. 반대로 §5.3에서 예고했던 `Math.max` equivalent의 교차 확인은 불발 — 그 파일(`product-list-results.tsx`)은 통합 분류라 mutate 범위 밖이다. 대신 도구가 자체 equivalent(3번)를 내놓아 "죽일 수 없는 변형" 기록은 성립했다.

**상시 사용 판단 — check에는 편입하지 않는다.** 25초면 싸지만, 뮤테이션 테스트는 회귀 감지가 아니라 **테스트 스위트 품질 감사**라 실행 주기가 다르다. 커밋마다 돌 이유가 없고, 테스트를 추가·개편할 때와 주기 감사 때 `pnpm exec stryker run`으로 수동 실행한다. 단위 분류 파일이 늘면 mutate 목록을 함께 늘리는 것을 유지 규칙으로 남긴다. 의존성·설정은 리포에 유지해 재현 가능하게 한다.
