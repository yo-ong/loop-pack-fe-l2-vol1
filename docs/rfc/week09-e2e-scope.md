# RFC: Week 09 — E2E 범위: 인증과 비싼 테스트를 어디에 붙일 것인가

브랜치 `feat/week-09` · 상태: 1·2단계 구현 완료, 3단계 설계 · 작성 2026-09-04

**이 문서의 구성** — A절은 2단계에서 정한 이벤트 스키마, B절은 시드 로그 분석, C절은 그 근거로 정한 E2E 범위다. 이 문서는 E2E 코드보다 먼저 커밋한다(과제 규칙). 4단계(구현)는 C절의 표만 만들고, 5단계(자가 검증) 기록은 부록으로 뒤에 붙인다.

E2E 한 번은 production build 위에서 mock API 500ms 고정 지연을 실제로 기다린다. 통합 테스트가 수백 ms 에 끝나는 일을 E2E 는 수십 초에 하므로, **로그가 말하는 곳과 로그가 말하지 못하는 곳을 갈라 붙일 자리를 정한다.**

**빨리 훑을 때** — 노이즈 기준 §B.1, 필터 전후 표 §B.2, 붙일 곳·안 붙일 곳 §C.1~C.3, 유지보수 비용 §C.4.

---

## A. 이벤트 스키마

### A.1 이름 규칙과 경계

- **이름은 시드 로그 스키마를 그대로 쓴다.** `snake_case`, `<대상>_<행위>`. 계측 스키마는 팀이 합의하는 것이라 이미 있는 이름에 맞추는 것이 목적이다. 새 이름을 만들지 않았다.
- **이름과 프로퍼티 타입은 `src/shared/analytics/events.ts` 한 곳에 있다.** 화면 코드는 `trackEvent(name, props)` 만 부르고, 이름·프로퍼티가 스키마에 없으면 타입 에러다. `button_click` 같은 이름이 들어올 자리가 없다.
- **컴포넌트는 스타터 로거(`src/analytics/*`)를 직접 부르지 않는다.** `shared/analytics` 한 겹만 부른다. eslint 하네스에 `@/analytics/*` 를 shared 밖에서 금지하는 규칙을 추가했다(`eslint.config.mjs`). 한 겹을 둔 이유는 두 가지다 — (1) 스키마 강제, (2) 나중에 실제 도구(GA4·Amplitude)를 붙일 때 `setupAnalytics()` 의 provider 목록만 바꾸면 된다.
- 스타터 로거는 손대지 않았다(`git diff 39707de6 -- src/analytics` 빈 diff).

### A.2 공통 프로퍼티 (`setCommonProperties`)

| 키          | 값                                                      | 근거                                                                                                                        |
| ----------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `sessionId` | `s_` + `crypto.randomUUID()`, `sessionStorage` 에 보관  | 탭 하나가 한 세션 — 탭을 닫으면 끝나고, 새로고침엔 유지된다. 접두사만 시드와 맞춘다. 집계 키라 4자리로는 충돌한다(부록 D-5) |
| `device`    | `mobile`(≤767) · `tablet`(≤1023) · `desktop`, 뷰포트 폭 | 시드의 세 값만 만든다. `null` 은 만들지 않는다(§B.1 — null 은 봇 신호였다)                                                  |
| `ts`        | ISO 8601 UTC                                            | 시드와 같다. 이벤트 발생 시점에 평가된다                                                                                    |
| `userId`    | identify 이후에만 붙는다                                | 시드와 같다 — "로그인한 뒤의 이벤트에만 있습니다"                                                                           |

### A.3 계측 지점과 시드 매핑

| 시드 이벤트                                              | 우리 구현                     | 발생 위치                                                                   | 프로퍼티 차이                                                                                    |
| -------------------------------------------------------- | ----------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `product_list_view`                                      | 동일                          | `_pages/products/ui/product-list-content.tsx` 마운트 1회                    | `+hasQuery`(검색어 유무). 필터·정렬·페이지 변경은 다시 보내지 않는다                             |
| `product_detail_view`                                    | **미구현 — 상세 화면이 없다** | —                                                                           | 시드 2위 경로지만 이 코드베이스에 화면이 없다. 만들지 않고 근거만 남긴다(§C.2)                   |
| `cart_add`                                               | 동일                          | `features/toggle-cart/ui/cart-button.tsx`, 담김으로 바뀔 때만               | 동일 `{productId, quantity:1}`. 해제는 시드에 없어 계측하지 않는다                               |
| `wishlist_add`                                           | 미구현(필수 지점 아님)        | —                                                                           | 다음 스프린트 후보                                                                               |
| `login_start`                                            | 동일                          | `features/auth/ui/login-form.tsx` 마운트                                    | `from` 을 복원 경로(next)의 첫 세그먼트로 정한다: `cart`·`orders`·`mypage`·`direct`              |
| `login_success`                                          | 동일                          | `features/auth/model/use-login.ts` onSuccess                                | `from` 동일. `identify` 직후라 `userId` 가 붙는다                                                |
| `login_fail`                                             | 동일                          | 같은 파일 onError                                                           | `reason`: `INVALID_CREDENTIALS`(401) · `SERVER_ERROR`(5xx) · `NETWORK`                           |
| `order_start`                                            | 동일                          | `_pages/checkout/ui/checkout-page.tsx` 마운트, 장바구니가 비어 있지 않을 때 | 시드 `{productId}` → 우리 `{productIds[], itemCount}`. 장바구니가 여러 상품이라 단수로 못 적는다 |
| `order_complete`                                         | 동일                          | `features/place-order/model/use-place-order.ts` onSuccess                   | 시드 `{productId, totalPrice}` → `{orderId, productIds[], itemCount, totalPrice}`                |
| `category_filter_change` · `sort_change` · `page_change` | 미구현(필수 지점 아님)        | —                                                                           | 8주차 통합 테스트가 이미 동작을 지키는 경로. 계측은 다음 스프린트                                |
| `client_error`                                           | 미구현                        | —                                                                           | 에러 바운더리 훅업은 범위 밖                                                                     |

### A.4 identify · reset · initAnalytics 의 자리

- **`identify()`·`reset()` 은 세션 상태 전이가 유일한 출처다.** `entities/session/model/use-session.ts` 의 effect 가 `user.id` 가 바뀔 때 `identifyUser` / `resetUser` 를 부른다. 로그인 폼·로그아웃 버튼·만료 처리가 각자 부르지 않는다. 같은 값이면 무시하므로(`identifyUser` 는 현재 id 와 같으면 no-op) `useSession` 이 여러 곳에 마운트돼도 한 번만 나간다. 예외 하나 — `login_success` 이벤트에 `userId` 를 싣기 위해 `use-login.ts` 가 `identifyUser` 를 먼저 부른다. effect 는 그 뒤에 같은 id 를 보고 무시한다.
- **`initAnalytics()` 는 루트 `Providers` 안의 `<AnalyticsInit/>` effect 에서 부른다.** 이 위치를 택한 이유는 모든 라우트가 지나는 유일한 클라이언트 경계라는 것 하나다. 첫 화면의 진입 이벤트(`product_list_view` 등)가 이 effect 보다 먼저 `track()` 되어도(형제·자식 effect 순서에 따라 그럴 수 있다) 두 가지가 지켜진다 — (1) 로거 큐가 초기화 전 이벤트를 담아 두고 초기화 직후 순서대로 내보내므로 **손실이 없다**, (2) 공통 프로퍼티(`setCommonProperties`)와 프로바이더 등록은 `trackEvent` 가 첫 호출 때 동기로 보장하므로 **첫 이벤트에도 `sessionId·device·ts` 가 붙는다**. 즉 `<AnalyticsInit/>` 의 트리 안 위치는 결과에 영향을 주지 않는다(검토에서 "자식 effect 가 먼저라서 된다" 는 초안의 근거가 틀렸음을 확인하고, 순서에 의존하지 않도록 고쳤다).
- 진입 이벤트는 `useTrackOnMount` 가 **진입(enabled 구간)마다 한 번** 보낸다. dev StrictMode 의 effect 재실행에는 1회이고, 주문서에 머문 채 장바구니가 비었다 다시 차면 `order_start` 는 다시 나간다 — 퍼널 분모라 재진입을 누락하면 전환율이 부풀려진다(부록 D-6). 시드 로그의 "완전 동일 줄"(§B.1) 같은 재전송은 여전히 만들지 않는다.

### A.5 로그인부터 주문까지 예측되는 순서

문서만 읽고 예측할 수 있어야 한다(완료조건). 익명으로 `/products` 진입 → 담기 → 헤더 장바구니 → 로그인 → 주문서 → 주문하기:

```
product_list_view {category, sort, page, hasQuery}            (userId 없음)
cart_add          {productId, quantity:1}                     (userId 없음)
login_start       {from:"cart"}                               (userId 없음)
identify u7
login_success     {from:"cart"}                               userId:"u7"
order_start       {productIds:[…], itemCount}                 userId:"u7"
order_complete    {orderId, productIds, itemCount, totalPrice} userId:"u7"
```

로그아웃하면 `reset` 이 한 번 나가고 이후 이벤트에 `userId` 가 빠진다. 로그인 상태로 새 탭을 열면 서버가 준 초기 세션으로 `identify` 가 먼저 나간다. 익명으로 처음 들어온 탭에서는 `reset` 이 나가지 않는다(이미 익명이면 no-op). 2단계 런타임 검증(production build, 2026-09-04)에서 관측한 순서가 위와 정확히 같았다.

---

## B. 시드 로그 분석

`fixtures/events-30d.jsonl` — 22,427 이벤트, 8,000 세션, 2026-07-22 ~ 08-20. 집계는 스크립트로 했고 스크립트는 커밋하지 않는다(기준과 결과만 여기 남긴다).

### B.1 노이즈 기준

로그에 "봇이다" 라는 표시는 없다. 분포를 보고 세 가지를 정했다.

| 노이즈      | 기준                                                                                | 근거(실측)                                                                                                                                                                                                             | 제거량                |
| ----------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| 중복 전송   | 줄 전체(sessionId·ts·name·props·device·userId)가 완전히 같은 두 번째 줄             | 같은 세션에서 같은 이름·props 가 연속으로 나온 77쌍이 **전부 간격 0초**, 그리고 완전 동일 줄이 정확히 77개. 재전송이지 두 번 본 게 아니다                                                                              | 77 이벤트             |
| 봇 세션     | `device` 가 `null` 인 세션                                                          | 486 세션 전부 (1) 이벤트 1개 `product_list_view` 만, (2) **매일 01:00~01:20Z 에 몰림**(하루 16.2개, 다른 시각엔 0), (3) 필터·상세·담기 0. 사람 세션은 device 가 세션 안에서 바뀌는 경우가 0이라 세션 단위로 걸 수 있다 | 486 세션 · 486 이벤트 |
| 오류 이벤트 | `client_error` 는 경로가 아니라 실패 신호 — 경로 집계에서 빼고 실패 비용에서만 쓴다 | 103건 전부 `{code:"IMAGE_LOAD_FAILED", productId}`, 상세 진입 직후에 끼어 있다                                                                                                                                         | 103 이벤트            |

이벤트 빈도(평균 간격 <2초 = 12세션)나 세션 길이(최대 11 이벤트)로는 봇을 가를 수 없었다 — 봇이 "느리고 짧게" 행동한다. `device: null` 과 시각 집중이 유일한 신호였다.

### B.2 경로 표 — 세션 기준

**묶은 기준** — 이벤트 하나가 화면 하나인 것은 그대로, 목록 화면 안의 조작 세 개(카테고리·정렬·페이지)는 "목록 조작" 으로 묶었다. 같은 화면에 머무는 행동이고 8주차에 한 묶음으로 검증했기 때문이다.

**분모** — 비율은 **필터 후 사람 세션 7,514** 대비다. 필터 전 표의 분모는 8,000.

**이탈률 정의** — "그 경로를 지난 세션 중 **다음 단계 이벤트가 없는** 비율"(퍼널). 다음 단계: 목록·목록 조작→상세, 상세→담기 또는 찜, 담기→로그인 시작 또는 주문서, 로그인 시작→로그인 성공, 로그인 성공→주문서, 주문서→주문 완료. "세션의 마지막 이벤트였던 비율" 은 다른 값이 나온다(예: 로그인 시작 0% vs 9%) — 로그인 시작 뒤엔 성공/실패가 반드시 따라와서 마지막일 수 없기 때문에, 퍼널 정의를 쓴다.

**실패 비용** — 실패로 센 이벤트: `client_error`(상세), `login_fail`(로그인). 비율과 함께 "이 화면이 막히면 뒤따라 막히는 것" 을 쓴다.

| #   | 경로                            | 세션 수(후) | 비율(후) | 이탈률(퍼널) | 실패 비용                                                                                                               | E2E 여부(가설)                        |
| --- | ------------------------------- | ----------: | -------: | -----------: | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| 1   | 목록 진입                       |       7,514 |   100.0% |        38.0% | 막히면 전부 막힌다. 다만 8주차 E2E 13·14·15 가 이미 이 화면을 지난다                                                    | 추가 안 함(이미 있음)                 |
| 2   | 상세 진입                       |       4,656 |    62.0% |        64.5% | `client_error` 세션 2.2%(103/4,656). 오류 세션의 담기·찜 전환 34.0% vs 정상 35.6% — 이미지 실패는 전환을 거의 안 깎는다 | **화면 없음** → 자연 제외             |
| 3   | 목록 조작(카테고리·정렬·페이지) |       4,544 |    60.5% |        37.7% | 조작이 안 되면 찾기 실패. 8주차 통합 8~11 + E2E 13·14 가 지킨다                                                         | 안 함(통합으로 충분)                  |
| 4   | 담기                            |       1,309 |    17.4% |        50.8% | 담기 → 로그인 시작 49.2%. 막히면 주문이 시작되지 않는다. 8주차 E2E 15(찜, 동형)·통합 12 가 지킨다                       | 안 함(단독으로는). 주문 플로우 앞부분 |
| 5   | 로그인 시작                     |         644 |     8.6% |         9.0% | `login_fail` 세션 18.8%(121/644), 그중 63 세션만 재시도 성공. **막히면 주문·마이페이지 전부 못 쓴다**                   | **필수**                              |
| 6   | 로그인 성공                     |         586 |     7.8% |        28.5% | 로그인 관문 뒤 세션 전체(7.8%)가 이 위에 선다                                                                           | 필수(5와 한 시나리오)                 |
| 7   | 위시리스트 추가                 |         480 |     6.4% |            — | 8주차 E2E 15 가 그대로                                                                                                  | 안 함(이미 있음)                      |
| 8   | 주문서 진입                     |         419 |     5.6% |        33.9% | 주문서 → 완료 66.1%. **유일한 매출 경로**. 8주차에 검증 없음(화면이 이번 주에 생겼다)                                   | 붙임(가설) → C.1 ② 채택               |
| 9   | 주문 완료                       |         277 |     3.7% |            — | 서버 주문 저장 → 주문 내역 재조회 왕복. 8주차에 검증 없음                                                               | 붙임(가설, 8과 한 시나리오) → 채택    |
| 10  | 로그인 실패                     |         121 |     1.6% |        47.9% | 실패 후 절반이 그대로 떠난다 — 실패 문구가 안 보이면 재시도도 없다                                                      | 필수(인증 세 갈래 중 하나)            |

### B.3 필터 전후 비교

| 경로       | 세션(전 → 후) | 순위(전 → 후) | 이탈률(전 → 후) |
| ---------- | ------------- | ------------- | --------------- |
| 목록 진입  | 8,000 → 7,514 | 1 → 1         | 41.8% → 38.0%   |
| 나머지 9개 | 변화 없음     | 변화 없음     | 변화 없음       |

**순위는 하나도 바뀌지 않았다.** 이유는 봇이 목록 진입 하나만 남기고 떠났기 때문이다 — 486 세션이 모두 1위 경로에만 더해졌고 1위는 이미 압도적(2위의 1.6배)이었다. 중복 77줄은 세션 기준 집계에서는 애초에 세지지 않는다(같은 세션이 같은 경로를 두 번 지나도 1세션). **바뀐 것은 이탈률이다** — 목록 진입 이탈률이 3.8%p 부풀려져 있었다. 봇을 안 걸렀다면 "목록에서 상세로 못 넘어가는 사람이 42%" 라고 읽었을 것이다. 순위가 안 바뀐 것도 결과다: 이 로그에서 노이즈는 순위가 아니라 **비율을 왜곡** 한다.

---

## C. 붙일 곳과 안 붙일 곳

E2E 시나리오는 **인증 + 최대 2개** 다. B 표를 놓고 고른 결과는 **인증 + 주문 완료 플로우, 총 2개** 다. 시나리오 선택은 AI 가 아니라 작성자가 했다 — AI 는 B 표의 집계와 후보의 장단점을 정리했고, 어느 것을 붙일지는 표를 보고 직접 정했다(하나도 안 붙이는 선택지와 로그아웃 정리를 셋째 시나리오로 두는 선택지를 놓고 비교했다).

### C.1 붙이는 곳

**① 인증 플로우 — 필수, 빈도는 낮은데 붙인다.** 로그인 시작은 사람 세션의 8.6% 에서만 일어난다. 빈도만 보면 10개 중 5위다. 그런데 로그인 관문 뒤에 주문·마이페이지가 전부 서 있고, 로그인이 막히면 그 뒤의 세션 7.8% 가 통째로 0 이 된다. 로그만 보고 정할 수 없는 이유가 하나 더 있다 — **인증의 핵심 동작은 브라우저와 서버의 경계에 산다.** proxy 리다이렉트(요청이 페이지에 닿기 전), httpOnly 쿠키(JS 가 못 본다), 서버 렌더된 초기 HTML 의 로그인 상태, 로그인 후 `router.refresh` 와 라우터 캐시 — jsdom 은 이 중 어느 것도 재현하지 못한다. 1단계 런타임 검증에서 **통합 테스트 145개가 전부 초록인데 브라우저에서만 재현되는 결함 2개**(로그인 후 주문서 복귀 실패 — prefetch 캐시, 주문 후 이동 유실 — 언마운트 타이밍)가 나왔다. 그 두 개가 이 시나리오의 존재 이유다.

세 갈래(과제 요구):

| 갈래                                     | 재현                                                             | 단언                                                                                                            |
| ---------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 보호 경로 진입 → 로그인 → 원래 경로 복원 | 미로그인으로 `/orders` 진입                                      | URL `/login?next=%2Forders` → 폼 로그인 → URL `/orders`, 헤더 `루퍼N님`                                         |
| 세션 만료 경계                           | `storageState` 로그인 + `scenario=expired` 쿠키로 `/mypage` 진입 | URL `/login?next=%2Fmypage&reason=expired`, alert `세션이 만료되었어요`, 헤더에 `로그인` 링크(죽은 쿠키 정리됨) |
| 잘못된 자격 증명                         | 틀린 비밀번호                                                    | alert `이메일 또는 비밀번호를 확인해주세요.`, URL 은 `/login` 유지                                              |

덧붙이는 경계 두 개(같은 파일, 개수를 늘리는 게 아니라 세 갈래의 경계): 복원 경로가 외부 주소(`next=https://evil.com`)면 `/` 로 떨어진다 · JavaScript 를 끈 컨텍스트(`javaScriptEnabled: false`)로 `/mypage` 를 열어도 헤더에 인사말·로그아웃이 보이고 로그인 링크가 없다(초기 HTML 의 로그인 상태). 익명 컨텍스트에는 로그인 링크가 있어야 한다 — 헤더가 서버에서 아예 렌더되지 않아도 앞 단언이 통과하는 일을 막는 양성 단언이다.

**② 주문 완료 플로우 — 로그 근거로 고른 하나.** 담기 → 헤더 장바구니 → (로그인 상태) 주문서에 상품·금액 → 주문하기 → 주문 내역에 새 주문 1건 추가. 세션 비율 3.7% 로 낮지만 유일한 매출 경로고, 8주차에 검증된 적이 없으며(화면이 이번 주에 생겼다), 통합 테스트가 못 보는 것이 둘 있다 — (a) 주문 저장소가 Route Handler 프로세스 메모리에 있어 **저장 → 재조회 왕복은 실제 서버에서만** 성립하고, (b) 주문 성공 후 `router.push("/orders")` 가 장바구니 비움과 경쟁하는 타이밍은 1단계에서 실제로 유실됐던 결함이다. 상대 개수로 단언한다(실행 누적 대비 +1건).

### C.2 안 붙이는 곳

- **목록 조작(2·3위, 60.5%) — 빈도는 높은데 E2E 를 안 붙인다.** 이 경로가 지키려는 사실은 "조작 ↔ 요청 파라미터 ↔ 목록 교체" 의 계약이고, 8주차 통합 8~11 이 `NuqsTestingAdapter` + MSW 로 정확히 그것을 지킨다. 브라우저만 가진 사실(히스토리·reload)은 이미 E2E 13·14 가 지난다. 여기 E2E 를 더 붙이면 같은 사실을 100배 비싸게 두 번 지키는 것이다. "빈도 1위니까 E2E" 는 8주차에 이미 무엇으로 검증했는지 확인하면 답이 나온다.
- **상세 진입(2위, 62%)** — 화면이 없다. 로그 스키마가 구현보다 앞서 있는 경우로, 없는 화면을 만들지 않는다. 계측(A.3)에서도 뺐다.
- **목록 진입 → 담기 스모크** — 8주차 E2E 15 가 찜 버튼으로 동형 경로를 지난다. 담기는 ②의 첫 단계로 한 번 지나가면 충분하다.
- **위시리스트** — E2E 15 그대로.

### C.3 4단계에 들어갈 테스트 (이 문서만 읽고 예측 가능해야 한다)

| 파일                         | storageState | 테스트                                                                                                                                       |
| ---------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `e2e/auth.spec.ts`           | **안 씀**    | 4개: 복원, 외부 주소 차단, 자격 증명 실패, 초기 HTML 로그인 상태(JS 비활성 컨텍스트)                                                         |
| `e2e/session-expiry.spec.ts` | 씀           | `scenario=expired` → 만료 안내 · 죽은 쿠키 정리                                                                                              |
| `e2e/order-flow.spec.ts`     | 씀           | 담기 → 주문서 → 주문하기 → 주문 내역 +1. 끝에 로그아웃 → 헤더 `로그인` 링크(로그아웃 정리는 별도 시나리오로 늘리지 않고 여기서 한 번 지난다) |

로그인 자체를 검증하는 테스트가 `storageState` 를 쓰지 않는 이유: 검증 대상을 픽스처가 대신 해 버리면 테스트가 지키는 게 없다. 반대로 만료·주문은 "로그인된 상태" 가 전제라 워커당 1회 로그인을 재사용한다.

### C.4 유지보수 비용 추정 — 인증 시나리오

문구·구조가 바뀌면 몇 군데를 고치나:

| 변경                                             | 고칠 곳                                                                                                                                        | 개수      |
| ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| 폼 라벨(`이메일`·`비밀번호`)·버튼(`로그인`) 문구 | Page Object `e2e/pages/login-page.ts` 의 로케이터                                                                                              | 1파일 3줄 |
| 만료·실패 안내 문구                              | 단언 2곳(만료 1, 실패 1) — 문구를 상수로 두면 1곳                                                                                              | 2 → 1     |
| 복원 파라미터 이름(`next`·`reason`)              | 기대 URL 을 만드는 `e2e/pages/urls.ts` 한 곳. 구현의 상수를 import 하지 않는 이유는 구현이 바뀌면 기대값도 따라 바뀌어 회귀를 못 잡기 때문이다 | 1         |
| 로그인 폼이 페이지에서 모달로 바뀜               | POM 1파일 + URL 단언 3곳(`/login` 이 사라지므로)                                                                                               | 4         |
| 헤더 인사말 형식(`루퍼N님`)                      | `e2e/pages/header.ts` 의 `greeting()` 한 곳                                                                                                    | 1         |

셀렉터를 역할·이름으로 두고 문구·URL 상수를 공유하면 **가장 흔한 변경(문구)은 1파일**, 구조 변경은 4곳에서 멈춘다. 이 추정을 넘어가면 시나리오를 줄이거나 통합으로 내리는 신호로 본다.

---

# 부록 — 프로젝트 기록

부록은 4·5단계를 실행한 뒤 추가한 실측 기록이다. §A~§C 의 설계는 E2E 코드보다 먼저 커밋됐다(`b9d3c8dd`).

## 부록 A. 5단계 — E2E 가 무엇을 잡는지 (2026-09-04 실측)

규칙: 실험마다 인증 구현을 **한 곳만** 바꾸고, 테스트 코드는 건드리지 않는다. 매 실험 `pnpm build` → `playwright test --workers=4`(12개) → 관련 vitest → `git checkout` 원복. 망가뜨린 코드는 커밋에 남지 않았다(`git status` clean 확인).

| #   | 망가뜨린 곳                                      | 어떻게 바꿨나                                                                         | 결과         | 실패한 테스트                               | 실패 메시지로 원인을 알 수 있었나                                                                                                                                                                             | 통합·단위로도 잡히나                                                                                                                                                                                    |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------------------- | ------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `src/proxy.ts` 가드 조건                         | `has(cookie)` → `!has(cookie)` (있으면 로그인으로, 없으면 통과)                       | 잡힘         | E2E 4개: 복원, 초기 HTML, 만료, 주문 플로우 | 부분. 복원 테스트는 `Expected /orders · Received /login?next=%2Forders` 로 "로그인했는데 로그인으로 돌아왔다" 가 읽힌다. 나머지 3개는 `ERR_TOO_MANY_REDIRECTS` — 원인(가드 반전)이 아니라 증상(루프)만 보인다 | 예 — `src/proxy.test.ts` 3개 실패. 단, 미로그인 `/orders` 진입은 서버 페이지 가드가 2중으로 막아 첫 단언은 통과했다                                                                                     |
| 2   | `src/app/login/page.tsx`                         | `next` 파라미터를 읽지 않고 항상 `/` 로 복원                                          | 잡힘         | E2E 1개: 복원                               | 예. `Expected /orders · Received /` — 복원 경로가 버려졌다는 뜻이 그대로 읽힌다                                                                                                                               | **아니오 — E2E 만.** `LoginForm` 통합 테스트는 `returnTo` 를 props 로 받아 replace 호출만 보고, `next` 를 읽는 층은 서버 컴포넌트다. vitest 23개 전부 초록                                              |
| 3   | `features/auth/ui/session-boundary.tsx`          | 로그인으로 보낼 때 `reason=expired` 를 빼고 보냄                                      | 잡힘         | E2E 1개: 만료                               | 예. `Expected …&reason=expired · Received …/login?next=%2Fmypage` — 사유가 빠진 것이 URL 차이로 보인다                                                                                                        | 예 — `session-boundary.dom.test.tsx` 2개 실패                                                                                                                                                           |
| 4   | `src/app/layout.tsx`                             | 헤더에 서버 세션을 넘기지 않음(`initialUser={null}`) — 초기 HTML 에 로그인 상태 없음  | 잡힘         | E2E 2개: 초기 HTML, 만료                    | 예. 초기 HTML: JS 없는 컨텍스트에서 `greeting … element(s) not found`. 만료: `Received /mypage` — 세션 캐시가 비어 있어 401 이 만료로 판정되지 않고 화면에 머문 것까지 드러났다                               | **아니오 — E2E 만.** 헤더·세션 통합 테스트 12개 전부 초록. 컴포넌트 단위에서는 "layout 이 값을 안 넘긴 것" 을 볼 수 없다                                                                                |
| 5   | `features/place-order/model/use-place-order.ts`  | 뮤테이션 콜백 안에서 `clearCart()` 를 `onPlaced()` 앞으로                             | **살아남음** | 없음 (E2E 12 · vitest 2 전부 통과)          | —                                                                                                                                                                                                             | — 동치 뮤턴트. 뮤테이션 레벨 콜백은 언마운트와 무관하게 끝까지 실행되므로 순서를 바꿔도 관찰 가능한 동작이 같다. 주석이 "순서가 지킨다" 고 읽히던 것을 "뮤테이션 레벨에서 부르는 것이 지킨다" 로 고쳤다 |
| 5′  | `features/place-order/ui/place-order-button.tsx` | 1단계에서 실제로 있었던 결함 형태로 — 이동을 `mutate()` 호출 단위 `onSuccess` 로 옮김 | 잡힘         | E2E 1개: 주문 플로우                        | 예. `Expected /orders · Received /checkout` — 주문 뒤 이동이 없었다는 것이 읽힌다                                                                                                                             | **처음엔 아니오** — `place-order-button.dom.test.tsx` 는 버튼만 렌더해 언마운트가 일어나지 않아 통과했다. → `checkout-page.dom.test.tsx` 를 추가해 이제 통합에서도 잡힌다(부록 A.1)                     |
| 6   | `src/shared/lib/return-to.ts`                    | `"/"` 로 시작하는지 검사 제거(외부 절대 URL 허용)                                     | 잡힘         | E2E 1개: 외부 주소                          | 예. `Expected http://localhost:3000/ · Received https://evil.com/` — 오픈 리다이렉트가 URL 로 보인다                                                                                                          | 예 — `return-to.test.ts` 4개 실패                                                                                                                                                                       |

**E2E 만 잡을 수 있었던 항목: 2번(서버 컴포넌트의 복원 경로 읽기), 4번(초기 HTML 의 로그인 상태).** 둘 다 jsdom 이 재현하지 못하는 층(서버 컴포넌트 렌더, 서버가 보낸 HTML)에 산다. 1번(proxy)은 단위 테스트가 조건을 잡지만 matcher 해석과 실제 리다이렉트 루프는 E2E 만 본다.

### A.1 살아남은 것에 대한 대응

- **5번은 동치 뮤턴트** 라 테스트로 잡을 수 없고 잡을 필요도 없다. 대신 코드 주석이 잘못된 인과("순서가 지킨다")를 말하고 있어 고쳤다.
- **5′ 은 통합 테스트의 공백** 이었다. 버튼 컴포넌트만 렌더하는 테스트는 "주문 성공 → 장바구니 비움 → 주문서가 빈 상태 분기로 바뀜 → 버튼 언마운트" 를 재현하지 못한다. `src/_pages/checkout/ui/checkout-page.dom.test.tsx` 를 추가했다 — 주문서 전체를 렌더하고 주문 뒤 빈 화면으로 바뀐 다음에도 `router.push("/orders")` 가 호출됐는지 본다. 5′ 뮤턴트를 다시 넣고 돌려 실패하는 것을 확인했다.
- **통합으로도 잡히게 된 5′ 을 두고 주문 플로우 E2E 를 남길 이유가 있나** — 있다. 이 시나리오가 지키는 사실 중 "주문 저장 → 주문 내역 재조회에서 +1" 은 Route Handler 프로세스 메모리의 주문 저장소를 실제로 거쳐야 하고, MSW 는 그 왕복을 흉내낼 뿐이다(핸들러가 `{orders: []}` 를 돌려주도록 우리가 정한다). 이동 유실은 통합으로 내렸고, E2E 에는 서버 왕복과 로그아웃 뒤 재잠금이 남는다.
- 3·6번처럼 **통합·단위가 함께 잡는 항목** 은 E2E 의 존재 이유가 아니다. 그 둘은 인증 시나리오의 경계 케이스로 같은 파일에 얹혀 있어 추가 비용이 수 초라 남겼다. 인증 E2E 가 이 둘만 잡는 시나리오였다면 지웠을 것이다.

## 부록 B. 4단계 — 격리 · 결정론 · trace 기록

### B.1 무엇을 워커별로 갈랐나

| 대상          | 방식                                                                                                  | 근거                                                                                                              |
| ------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 계정          | `parallelIndex` → `looper{N+1}` (`e2e/fixtures/auth.ts`). 계정 목록은 mock 백엔드의 `accounts` 그대로 | 주문 저장소가 계정별 `Map` 이라 계정만 가르면 데이터가 갈린다. 계정 8개 = 워커 상한 8, 넘으면 픽스처가 throw 한다 |
| 스토리지      | `test-results/.auth/worker-{parallelIndex}.json`, 실행마다 새로 로그인                                | 파일을 갈라야 storageState 가 섞이지 않는다. TTL 1시간이라 디스크 재사용은 하지 않는다                            |
| 데이터        | 주문 개수는 "내 계정 +1" 상대 단언                                                                    | 서버 프로세스가 실행 사이에 살아 있으면 주문이 누적된다                                                           |
| 장바구니·위시 | 갈 필요 없음                                                                                          | persist 가 없어 storageState 에 실리지 않는다 — 모든 테스트가 0 에서 시작한다                                     |

세션은 무상태 HMAC 이라 만료 테스트의 "죽은 쿠키 정리(logout)" 와 주문 플로우의 로그아웃은 **그 컨텍스트의 쿠키만** 지운다. 워커 저장 파일과 다른 테스트의 토큰은 그대로다.

### B.2 결정론 (2026-09-04, production build)

| 명령                         | 결과                         | 시간     |
| ---------------------------- | ---------------------------- | -------- |
| `--workers=4` × 3회 연속     | 12 passed × 3                | 7.8~7.9s |
| `--workers=1`                | 12 passed                    | 18.3s    |
| `--workers=8` × 15회         | 12 passed × 14, **1회 실패** | 7.2~8.8s |
| (경합 수정 후) `--workers=4` | 12 passed                    | 8.0s     |

통과 집합은 워커 수와 무관하게 같은 12개다. **8워커 15회 중 1회 실패는 테스트가 아니라 앱의 경합이었다** — 로그인 화면이 마운트되면서 미인증 세션 재확인(`GET /api/auth/me`, 500ms 지연)이 나가고, 그 401 이 도착하기 전에 로그인이 성공해 세션 캐시가 채워지면 `SessionBoundary` 가 뒤늦은 401 을 "로그인 상태였는데 401 = 만료" 로 읽어 `/login?reason=expired` 로 되돌렸다. 워커 8개가 단일 `next start` 를 두드릴 때 `me` 응답이 밀려 창이 넓어졌다. 수정: 로그인 성공 시 진행 중인 세션 쿼리를 `cancelQueries` 로 취소하고(`use-login.ts`), `getSession` 이 `AbortSignal` 을 받아 요청 자체를 중단한다. 같은 순서를 jsdom 에서 재현하는 회귀 테스트를 `login-form.dom.test.tsx` 에 두었다. 통합 테스트 전부가 초록인 상태에서 병렬 E2E 만이 드러낸 결함이라, C.1 의 "인증은 로그만 보고 정할 수 없다" 는 근거가 하나 더 늘었다.

### B.3 trace 를 직접 읽은 기록

1. `e2e/auth.spec.ts` 의 자격 증명 실패 테스트에서 기대 문구를 `"비밀번호가 틀렸습니다."` 로 고의로 틀리게 바꿨다.
2. `pnpm exec playwright test e2e/auth.spec.ts --trace=on --workers=1` → 1 failed, `test-results/auth-자격-증명이-틀리면-…-chromium/trace.zip` 생성.
3. `pnpm exec playwright show-trace <그 경로>` 로 연다. 액션 6개 중 `expect.toHaveText` 하나만 빨갛다 — `newPage`·`goto /login`·`fill`×2·`click` 은 전부 44ms 이내에 끝났고, 네트워크 탭에 `POST /api/auth/login → 401` 이 t+150ms 에 시작해 t+655ms 에 끝나 있다. 실패한 단언은 5,010ms 를 기다리며 9번 폴링했는데 매번 alert 요소 `이메일 또는 비밀번호를 확인해주세요.` 를 찾았다.
4. **읽은 것** — "느려서 못 기다린" 것이 아니다. 단언이 시작될 때 서버 응답과 alert 렌더는 이미 끝나 있었고, 셀렉터도 맞았으며, 기대 문자열만 틀렸다. 앱을 고칠 일이 아니라 기대값을 고칠 일이라는 판단이 trace 한 장에서 나온다. 단언을 원래대로 되돌렸다.

### B.4 8주차 환경에서 바꾼 것

- `reuseExistingServer: !CI` → `false`. 로컬에 `next dev` 가 3000 에 떠 있으면 dev 빌드 위에서 조용히 돌 수 있었다. 이제 항상 production 서버를 Playwright 가 직접 띄우고, 포트가 점유돼 있으면 재사용 대신 실패한다.
- `workers: isCI ? 2 : 4`, `retries: 0`, `trace: "retain-on-failure"`, `fullyParallel: true` 추가. 재시도를 0 으로 둔 것은 B.2 의 경합처럼 **간헐 실패를 재시도로 덮지 않기 위해서** 다.

## 부록 C. Advanced B — 에이전트 생성 테스트 검수 (2026-09-04)

### C.1 어떻게 돌렸나

`pnpm exec playwright init-agents --loop=claude` 가 만든 것: `.claude/agents/playwright-test-{planner,generator,healer}.md`, `.mcp.json`(`playwright-test` MCP 서버), `specs/README.md`, 시드 `seed.spec.ts`. 시드는 `e2e/` 에 생겼는데 그대로 두면 `pnpm check` 의 E2E 개수가 13개가 되므로 `e2e-generated/` 로 옮겼다 — **생성물은 전부 `e2e-generated/` 에 두고 `testDir`(`./e2e`) 밖에 남긴다.** 린트·타입 체크는 받지만 게이트에서 실행되지 않는다. 3단계 표에 없는 시나리오를 늘리지 않기 위해서다.

**대체한 것 하나** — 이 세션의 Claude Code 에는 새 에이전트 정의가 로드되지 않았고 `mcp__playwright-test__*` 도구도 없었다. 그래서 planner·generator 정의 파일의 프롬프트를 범용 에이전트(opus)에 그대로 주고, 브라우저 조작(`browser_snapshot` 등)만 Playwright 스크립트의 `ariaSnapshot()` 으로 대신하게 했다. planner 는 `specs/auth-and-order.md`(4 스위트 · 14 시나리오)를, generator 는 그중 내 4단계 범위와 겹치는 8개를 `e2e-generated/*.spec.ts` 로 만들었다(둘 다 `e2e/`·`docs/rfc/` 를 읽지 못하게 했다 — 독립적으로 만든 것을 비교하려고). 생성 직후 8개 모두 통과(2 워커, 7.1~7.7초).

### C.2 내 테스트와 비교

| 관점          | 생성물(8개)                                                                                    | 내 4단계 테스트(6개)                               | 판단                                                                                                                   |
| ------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 시나리오 선택 | 로그인 성공 자체, 로그아웃 후 재잠금, 게스트 장바구니의 로그인 후 유지 등 **범위 밖 3개 포함** | RFC C 표의 것만                                    | 생성물은 "할 수 있는 것" 을 다 만든다. 로그만 보고 뺐던 시나리오가 생기는 이유가 여기 있다 — 근거 없이 개수가 늘어난다 |
| 로그인 재사용 | 테스트마다 폼으로 로그인(8회). storageState 없음                                               | 워커당 1회 storageState, 로그인 검증 테스트만 예외 | 생성물은 테스트 간 공유 구조를 만들지 못한다. 8개에 8번 로그인 = 실행 시간의 절반이 로그인이다                         |
| 셀렉터        | role·name 기반, `getByRole("banner")`·`region {exact:true}` 스코프 — 좋다. testId 없음         | Page Object(`LoginPage`·`Header`) + role·name      | 셀렉터 품질은 대등. 다만 8개 파일에 같은 헤더 스코프 코드가 8번 복사돼 문구가 바뀌면 8곳을 고친다(내 것은 1곳)         |
| 단언 대상     | 픽스처 이름·가격 리터럴(`WOOD GLOVES`, `52,000원`), 쿠키 이름, 절대 URL 포함                   | 화면에서 읽은 값을 앞뒤로 비교, 상대 개수          | 아래 C.3 에서 지웠다                                                                                                   |
| 격리          | 계정 고정(looper5·6). 워커 계정과 겹치면 같은 계정의 주문이 섞인다                             | `parallelIndex` 계정                               | 생성물은 병렬 격리를 모른다 — 8워커에서 돌리면 내 order-flow(looper5=워커4) 와 같은 계정을 쓴다                        |
| 배운 것       | `getByRole("region", { name, exact: true })` 로 "불러오는 중" 라벨과 완료 라벨을 가르는 수법   | 나는 `총 N개` 텍스트 대기를 썼다                   | 이 수법은 내 테스트에도 적용할 만하다(다음 스프린트)                                                                   |

### C.3 지운 단언과 이유

| 파일                                                                  | 지운 것                                                                                                         | 이유                                                                                                                                                                                                       |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `order-cart-items-and-clear-cart`                                     | `"WOOD GLOVES 장바구니"`·`"네로 비니 블랙 장바구니"` 버튼 이름, `3,000원`·`49,000원`·`52,000원`·`합계 52,000원` | **픽스처 이름·가격 단언**(8주차 RFC B-4 금지). 상품 데이터가 바뀌면 앱은 멀쩡한데 테스트만 깨진다 → 처음 두 카드의 제목을 화면에서 읽어 주문서·주문 내역에서 다시 찾고, 금액은 `[\d,]+원` 형식만 본다      |
| `guest-cart-survives-login`                                           | `"WOOD GLOVES 장바구니"`, `총 결제 금액 3,000원`                                                                | 같은 이유                                                                                                                                                                                                  |
| `external-next-url-is-not-followed`                                   | `expect(page.url()).not.toContain("evil.com")`                                                                  | 바로 앞의 `toHaveURL("/")` 가 같은 사실을 더 강하게(자동 재시도로) 단언한다. 재시도 없는 `page.url()` 스냅샷은 앞 단언이 통과한 뒤엔 항상 참인 동어반복                                                    |
| `expired-session-redirects-to-login`, `logout-blocks-protected-route` | `context.cookies()` 에 `session` 이 없다                                                                        | **구현 세부 단언.** 쿠키 이름은 사용자가 보는 것이 아니고, 사용자에게 보이는 사실(헤더 `로그인` 링크, 보호 경로 재방문 시 로그인으로)은 이미 단언돼 있다. 쿠키 이름을 바꾸면 앱은 멀쩡한데 테스트가 깨진다 |
| `logout-blocks-protected-route`                                       | `heading "주문 내역"` 이 hidden                                                                                 | URL 이 `/login` 인 것을 단언한 뒤라 항상 참. 무엇을 지키는지 말하지 않는 단언                                                                                                                              |
| 8개 전부                                                              | `toHaveURL("http://localhost:3000/…")` → 상대 경로                                                              | `baseURL` 이 바뀌면(CI 포트, 미리보기 URL) 8파일이 깨진다. 설정의 baseURL 을 쓰는 상대 경로가 맞다                                                                                                         |

지운 뒤 8개 재실행 → 전부 통과(7.2초). 남긴 것: 헤더 로그인 상태의 양성·음성 쌍, alert 문구, 복원 URL — 내 테스트와 같은 사실을 지킨다.

### C.4 healer 를 쓰지 않은 이유

healer 의 임무는 "실패하는 테스트를 통과할 때까지 테스트 코드를 고치는 것" 이다(정의 파일: _Continue until the test runs successfully_). 이번 주에 E2E 가 빨간불이 된 경우는 전부 **앱이 틀린 것** 이었다 — 1단계의 prefetch 캐시·이동 유실, 4단계의 8워커 세션 경합, 5단계의 고의 훼손. healer 를 걸었다면 "기대값을 현재 동작에 맞추는" 방향으로 테스트를 고쳐 결함을 초록으로 덮었을 것이고, 그건 과제가 금지한 "통과시키려고 가드나 복원 로직을 지우기" 의 테스트 쪽 거울상이다. 5단계 표의 "잡힘" 은 healer 가 없어야 성립한다. 셀렉터가 진짜로 낡은 경우(문구·구조 변경)에만 쓸 도구이고, 그때도 diff 를 사람이 읽어야 한다.

### C.5 결론

생성물은 **탐색과 초안** 에 강하다 — 14개 시나리오를 30분에 뽑고 셀렉터 스코프 수법도 배웠다. 약한 곳은 **무엇을 지킬지 고르는 것** 이다: 범위 밖 시나리오를 늘리고, 픽스처 값을 단언하고, 구현 세부(쿠키)를 단언하고, 테스트 간 공유 구조(storageState·POM·격리)를 만들지 않는다. 그 부분이 이번 주 과제의 핵심("무엇에 돈을 쓸지")이라 사람이 정하는 게 맞다. 생성물은 `e2e-generated/` 에 검수 기록과 함께 남기고 게이트에는 넣지 않는다.

## 부록 D. 리뷰 피드백 반영 (2026-09-06)

제출 뒤 받은 피드백 — 필수 3, 권고 3, 리뷰 포인트 답변 2 — 를 항목마다 커밋 하나로 반영했다. 규칙은 5단계와 같다: 고치기 전에 그 결함을 재현하는 테스트를 먼저 빨간불로 만들고, 고친 뒤 초록불을 확인한다.

| #   | 구분   | 문제                                                                                                                                                                                                                                                                        | 수정                                                                                                                                                                                                                             | 테스트                                                                                                                                                                                     |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 필수   | 만료 판정 "직전에 세션이 있었나" 의 전제(로그인 화면에서는 캐시가 비어 있다)가 깨지는 경로가 있다 — `/login?reason=expired` 로 온 뒤 재로그인 → 뒤로가기 하면 세션이 살아 있는 채로 로그인 화면에 머물고, 거기서 비밀번호를 틀리면 그 401 을 만료로 읽어 `logout()` 을 돈다 | 기준은 두고 조건 하나를 얹었다. 로그인 mutation 에 `LOGIN_MUTATION_KEY` 를 붙이고 `SessionBoundary` 가 그 키의 401 을 제외한다(`hashKey` 비교). 로그인 실패는 폼 에러로만 남는다                                                 | `login-form.dom.test.tsx` — SessionBoundary + LoginForm 동시 마운트, 세션 캐시에 user 를 심고 비밀번호 오류 → alert 표시 · logout 미호출 · replace 미호출 · 캐시 유지. 수정 전 빨간불 확인 |
| 2   | 필수   | 만료 복원 주소에서 쿼리가 사라진다 — proxy 는 `pathname+search` 를 싣지만 서버 페이지는 `requireServerSession("/orders")` 로 경로를 고정해, 쿠키는 있고 토큰만 죽은 `/orders?page=2` 가 `next=%2Forders` 가 된다                                                            | `requireServerSession(pathname, searchParams)` — 페이지의 `searchParams` 를 받아 `URLSearchParams` 로 직렬화(배열은 키 반복). 세 보호 페이지가 넘긴다                                                                            | `session.test.ts` — 쿼리 보존 · 같은 키 반복 · 빈 쿼리엔 `?` 없음                                                                                                                          |
| 3   | 필수   | 로그아웃 요청이 실패하면 캐시도 안 비우고 이동도 없다. 만료 경로는 `logout().catch` 뒤 무조건 정리하는데 정책이 달랐다                                                                                                                                                      | `useLogout` 의 `onSuccess` → `onSettled`, 버튼도 `onSettled` 에서 이동. 클라이언트는 서버 응답과 무관하게 로그아웃 상태로 수렴하고, 서버 쿠키가 남았으면 다음 서버 렌더가 알려 준다                                              | `header-auth.dom.test.tsx` — logout 500 에도 캐시 null · 홈 이동 · 로그인 링크                                                                                                             |
| 4   | 권고   | `sanitizeReturnTo` 가 `//`·`/\` 는 막지만 `/..//evil.com`·`/.//evil.com` 은 통과한다. 지금은 같은 출처로 해석되지만 검사 의도를 우회하는 입력이다                                                                                                                           | 점 세그먼트(`.`·`..`)가 있으면 기본 경로로. 앱이 만드는 복원 경로에는 정규화가 필요한 세그먼트가 없으므로, 있다는 것 자체가 비정상 입력이다                                                                                      | `return-to.test.ts` — 두 값 + 경로 중간·끝의 점 세그먼트 4건, 점이 든 파일명·쿼리는 통과하는 양성 2건                                                                                      |
| 5   | 권고   | `sessionId` 가 36진수 4자리(약 168만 개). 세션 집계 키라 충돌하면 두 세션이 합쳐져 비율이 조용히 틀어진다                                                                                                                                                                   | `s_` + `crypto.randomUUID()`. 시드 형식과 맞추는 것(표기)과 집계 키의 유일성은 별개                                                                                                                                              | `analytics.dom.test.tsx` — 새 세션 200개가 전부 다른 값                                                                                                                                    |
| 6   | 권고   | `useTrackOnMount` 의 1회 가드가 `enabled` 재진입까지 막아, 주문서에 머문 채 장바구니가 비었다 다시 차면 `order_start` 가 안 나간다. 퍼널 분모가 줄어 전환율이 부풀려진다                                                                                                    | `enabled` 가 false 로 내려가면 가드를 되돌린다 — "마운트당 1회" 가 아니라 "진입(enabled 구간)당 1회". StrictMode 재실행은 cleanup 에서 되돌리지 않으므로 여전히 1회                                                              | `use-track-on-mount.dom.test.tsx` — false→true→false→true 에 2회, 프로퍼티 변경엔 재전송 없음                                                                                              |
| 7   | 리뷰 2 | 루트 layout 의 `cookies()` 가 세션과 무관한 `/select`·`/dialog`·`/performance-lab/inp` 까지 동적으로 만들었다(7주차 정적화 되돌림). 얻은 건 헤더 하나, 내준 건 전체 화면인데 전후 비교가 없었다                                                                             | `(commerce)` 그룹 layout 이 세션을 읽고 `SessionBoundary`·`HeaderAuth` 를 두며, 데모 3개는 `(lab)` 그룹(세션 없음). 헤더 마크업은 `SiteHeader(auth 슬롯)` 로 뽑아 두 layout 이 조합한다. 루트 layout 은 `html/body/Providers` 만 | 아래 Route 표. E2E 12개 4워커·1워커 통과(URL·문구 불변, 스펙 무수정)                                                                                                                       |

### D.1 Route 표 전후 (`pnpm build`, 2026-09-06)

| 라우트                                                       |  전  |               후               |
| ------------------------------------------------------------ | :--: | :----------------------------: |
| `/`                                                          |  ƒ   | ƒ (`force-dynamic`, 변화 없음) |
| `/products` · `/login` · `/checkout` · `/orders` · `/mypage` |  ƒ   |               ƒ                |
| `/api/*` 5개                                                 |  ƒ   |               ƒ                |
| `/select`                                                    |  ƒ   |             **○**              |
| `/dialog`                                                    |  ƒ   |             **○**              |
| `/performance-lab/inp`                                       |  ƒ   |             **○**              |
| `/_not-found`                                                |  ƒ   |             **○**              |
| `ƒ Proxy (Middleware)`                                       | 있음 |              있음              |

전: 16개 전부 ƒ. 후: 정적 4개 · 동적 12개. 세션을 읽는 범위를 헤더가 로그인 상태를 보여 줘야 하는 그룹으로 좁힌 결과가 표 한 장으로 남는다.

### D.2 리뷰 포인트 1 에 대한 후속

401 을 로그인 실패와 만료로 가르는 세 방법(요청 표식 / 인증 API 경로 / 직전 세션 상태) 중 우리는 셋째를 골랐고, 그 성립 조건("로그인 화면에서 캐시가 비어 있다")이 깨지는 경로가 실제로 있었다. 조건을 바꾸는 대신 첫째 방법을 한 조각 빌려 왔다 — 로그인 mutation 에만 표식(`mutationKey`)을 달고 그 요청의 401 은 제외한다. 표식을 빠뜨릴 수 있는 요청이 로그인 하나뿐이라 첫째 방법의 약점(누락)은 여기서는 문제가 되지 않는다.
